# AUTOGENERATED FILE: DO NOT EDIT
# Generated by the AgentRegistry CLI.

services:
  echoagent:
    image: docker.io/shashankram/echoagent:0.0.1
    build:
      context: .
      dockerfile: Dockerfile
    command: ["echoagent", "--local"]
    ports:
      - "8080:8080"
    environment:
      # TODO: remove kagent variables (currently required)
      - KAGENT_URL=placeholder
      - KAGENT_NAME=placeholder
      - KAGENT_NAMESPACE=placeholder
      - AGENT_NAME=echoagent
      - OTEL_SERVICE_NAME=echoagent
      - OTEL_EXPORTER_OTLP_TRACES_ENDPOINT=${OTEL_EXPORTER_OTLP_TRACES_ENDPOINT}
      - MODEL_PROVIDER=agentgateway
      - MODEL_NAME=gpt-4o-mini
    volumes:
      - type: bind
        source: ./echoagent
        target: /config
